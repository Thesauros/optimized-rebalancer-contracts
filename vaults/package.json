{
  "name": "vaults",
  "version": "0.1.0",
  "description": "Thesauros Vaults Subgraph",
  "repository": "https://github.com/thesauros/vaults-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ vaults",
    "create-local": "graph create --node http://localhost:8020/ vaults",
    "remove-local": "graph remove --node http://localhost:8020/ vaults",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 vaults"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.34.0"
  }
}
